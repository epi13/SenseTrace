# worker-03 Phase 0 Evidence

This record captures the compact remote campaign run after deploying commit
`1aa458a562f1c017717d1e65801857981c4f4720` to `worker-03`.

## Campaign

- Run ID: `phase0-20260901T031717Z-8ad0b4cb`
- Configuration hash: `6e19e264bbe1f571c45c890d5da053263ae4d06727176d6f8bcf500a843a65a7`
- Conditions: `null`, `injected`, `shuffled`
- Rows per condition: 6,000 (`0`: 3,000; `1`: 3,000)
- Models: logistic regression and boosted trees
- Grouped split keys: `session_id`, `device_id`, `row_id`, `cell_or_offset_id`
- Test samples per model run: 900
- Training seeds: `11`, `23`, `37`, `53`, `71`

| Condition | Model | Balanced accuracy | AUROC | Parameters |
| --- | --- | ---: | ---: | ---: |
| null | logistic regression | 0.5015 | 0.5078 | 70 |
| null | boosted trees | 0.5425 | 0.5535 | 450 |
| injected | logistic regression | 0.5756 | 0.6158 | 70 |
| injected | boosted trees | 0.5898 | 0.6225 | 450 |
| shuffled | logistic regression | 0.4944 | 0.5045 | 70 |
| shuffled | boosted trees | 0.5040 | 0.5197 | 450 |

All model evaluations include bootstrap 95% confidence intervals, split and
dataset fingerprints, class balance, confusion matrices, model hashes, and
repeated-seed records. The acceptance checks passed: null and shuffled labels
were consistent with chance, the injected signal was detected, grouped split
metadata was visible, and identity features were rejected by default.

## Recovery and service acceptance

`sensetrace host verify-recovery worker-03` passed. The bounded interruption
test resumed from sample index 13 and completed at 32, quarantining the
deliberately incomplete temporary shard. The service process-restart test
killed only the runner process (`SIGKILL`), observed a new PID, and returned to
`active` (`NRestarts=1`).

The live deployment is the unprivileged user-service fallback:

- user service: active;
- system service: inactive;
- root disk: approximately 214 GiB free, 7% used.

The host account does not grant non-interactive sudo. Therefore this record
does not claim root systemd installation, kernel sysctl application, watchdog
configuration, headless boot, or reboot persistence. Those steps remain a
separate privileged acceptance test.

## Claim boundary

This campaign supports only the narrow claim that the known synthetic
perturbation was recovered under the recorded grouped holdout. It is not
evidence of physical DRAM-state inference.
