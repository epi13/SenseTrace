# Applied only by the privileged installer after SSH access has been validated.
# A kernel panic or oops then reboots after a short delay. This does not cover
# hard hangs, power interruption, or firmware power-loss policy.
kernel.panic = 10
kernel.panic_on_oops = 1
